require 'spec_helper'

describe EasySerializer do
  it 'has a version number' do
    expect(EasySerializer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
